# Подключение к общему PostgreSQL (сеть ShopBot). Отдельного сервиса db нет.
services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    networks:
      - shop-bot_default

networks:
  shop-bot_default:
    external: true
